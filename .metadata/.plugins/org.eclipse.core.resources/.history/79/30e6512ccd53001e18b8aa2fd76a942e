#include <stdio.h>
#include <math.h>

float a, b, q1, q2, q3, c;

int main(){
	printf("Digite um valor para a e b: ");
	scanf("%f%f", &a, &b);

	q1 = (a * a);
	q2 = (b * b);
	q3 = (q1 + q2);
	c = sqrt(q3);


	printf("%.2f", c);

	return 0;

}

