#include <stdio.h>


int num;
float val;
char carac[1];
char str[51];

int main(){

	scanf("%d%f%s%[^\n]", &num, &val, &carac, &str);
	printf("%d%f%s%s\n", num, val, carac, str);
	printf("%d\t%f\t%s\t%s\n", num, val, carac, str);
	printf("%10d%10f%10s%9s", num, val, carac, str);


	return 0;

}

