#include <stdio.h>
#include <string.h>
#include <ctype.h>



int main(){

	int n, cont, letra;
	char placas[100];


	scanf("%d", &n);

	for(int j = 0; j < n; j++){

		cont = 1;
		letra = 0;

		scanf("%s", &placas);
		int len = strlen(placas);
		char placa[len + 1];

		for(int i = 0; i < len; i++){
			placa[i] = placas[i];
		}
		placa[len] = '\0';

		size_t tam = sizeof(placa) / sizeof(placa[0]);

		printf("%zu\n", tam);

		if(tam < 9 || tam > 9){
			printf("FAILURE\n");
		}
		if(tam == 9){
			for(int k = 0; k < 8; k++){
				if(k <= 2){
					letra = islower(placa[k]);
					if(letra == 1){
						printf("FAILURE\n");
						break;
					}
					if(placa[k] != 'A' || placa[k] != 'B' || placa[k] != 'C'
							|| placa[k] != 'D' || placa[k] != 'E' || placa[k] != 'F'
							|| placa[k] != 'G' || placa[k] != 'H' || placa[k] != 'I'
							|| placa[k] != 'J' || placa[k] != 'K' || placa[k] != 'L'
							|| placa[k] != 'M' || placa[k] != 'N' || placa[k] != 'O'
							|| placa[k] != 'P' || placa[k] != 'Q' || placa[k] != 'R'
							|| placa[k] != 'S' || placa[k] != 'T' || placa[k] != 'U'
							|| placa[k] != 'V' || placa[k] != 'W' || placa[k] != 'X'
							|| placa[k] != 'Y' || placa[k] != 'Z'){
						cont = 0;
					}
				}
				if(k == 3 && placa[k] != '-'){
					cont = 0;
				}
				if(k > 3){
					if(placa[k] != '1' || placa[k] != '2' || placa[k] != '3'
						|| placa[k] != '4' || placa[k] != '5' || placa[k] != '6'
						|| placa[k] != '7' || placa[k] != '8' || placa[k] != '9'
						|| placa[k] != '0'){
						cont = 0;
					}
				}
			}
		}
		if(cont == 1){
			if(placa[8] == '1' || placa[8] == '2'){
				printf("MONDAY\n");
			}
			if(placa[8] == '3' || placa[8] == '4'){
				printf("TUESDAY\n");
			}
			if(placa[8] == '5' || placa[8] == '6'){
				printf("WEDNESDAY\n");
			}
			if(placa[8] == '7' || placa[8] == '8'){
				printf("THURSDAY\n");
			}
			if(placa[8] == '9' || placa[8] == '0'){
				printf("FRIDAY\n");
			}
		}
		if(cont == 0){
			printf("FAILURE\n");
		}

	}
	return 0;
}
