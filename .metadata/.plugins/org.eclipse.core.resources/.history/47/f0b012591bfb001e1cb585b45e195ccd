#include <stdio.h>


int main(){

	int vx = 60, vy = 90, distancia, minutos, diff;

	scanf("%d", &distancia);

	diff = vy - vx;

	minutos = (diff / distancia) * 60;

	printf("%d minutos", minutos);


	return 0;
}
