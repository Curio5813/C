#include <stdio.h>


int num;
float val;
char carac, str[49];


void entrada_e_saida(){

	scanf("%d%f%c", &num, &val, &carac);
	gets(str);
	printf("%d%.6f%s%s\n", num, val, carac, str);
	printf("%d\t%.6f\t%s\t%s\n", num, val, carac, str);
	printf("%10d%10.6f%10s%10s", num, val, carac, str);


}


int main(){

	entrada_e_saida();

	return 0;

}

