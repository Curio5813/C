#include <stdio.h>


int main(){

	long long int x, y, n, time = 0, i, k;

	long, long int data[2][3];

	for(i = 0; i < 2; i++){
		for(k = 0; k < 3; k++){
			printf("Digite um valor para %dº linha e %dº coluna: ", i + 1, k + 1);
			scanf("%d", &data[i][k]);
		}
	}

	return 0;

}
